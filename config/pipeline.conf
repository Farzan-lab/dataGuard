# ================================================
#  DataGuard — Pipeline Configuration
# ================================================


# ─── Paths──────────────────────────────────────

INCOMING_DIR="data/incoming"      # فایل‌های خام اینجا می‌افتن
VALID_DIR="data/valid
"         # فایل‌های تأییدشده
QUARANTINE_DIR="data/quarantine"    # فایل‌های رد شده
PROCESSED_DIR="data/processed"     # خروجی نهایی
LOG_DIR="logs"           # لاگ‌ها
REPORT_DIR="reports"        # گزارش‌های HTML


# ─── اعتبارسنجی ──────────────────────────────────

MIN_ROWS= 10            # حداقل تعداد ردیف قابل قبول
MAX_NULL_PCT= 50        # حداکثر درصد مقادیر خالی (عدد ۰ تا ۱۰۰)
AMOUNT_MIN= 0           # کمترین مقدار مجاز برای ستون amount
AMOUNT_MAX= 10000       # بیشترین مقدار مجاز برای ستون amount


# ─── تشخیص ناهنجاری ──────────────────────────────

ALERT_DROP_PCT= 20    # اگه فروش امروز X درصد کمتر از میانگین بود، هشدار بده


# ─── فرمت‌های قابل قبول ──────────────────────────

ACCEPTED_EXTENSIONS="csv json tsv API"   # مثلاً: csv json tsv


# ─── اطلاعات پروژه ───────────────────────────────

PROJECT_NAME="DataGuard"
VERSION="1.0.0"